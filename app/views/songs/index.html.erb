<h1> Welcome to Blah-Song </h1>

<table class="recent_songs">
  <row>
    <th>Title</th>
    <th>Artist</th>
  </row>
  <% @songs.each do |song| %>
  <row>
    <td><%= song.name %></td>
    <td><%= song.artist %></td>
  </row>
  <%end%>
</table>
