<h1> Welcome to Blah-Song </h1>

<table class="recent-songs">
  <tr>
    <th>Title</th>
    <th>Artist</th>
  </tr>
  <% @songs.each do |song| %>
  <tr>
    <td><%= song.name %></td>
    <td><%= song.artist %></td>
  </tr>
  <%end%>
</table>
